<!--components/Tabs/Tabs.wxml-->
<view class="tabs_container">
  <view class="tabs_item {{index == currentIndex ? 'active' : ''}}"
  wx:for="{{tabs}}"
  wx:key="id"
  bindtap = "handleChange"
  data-index="{{index}}"
  >{{item.name}}</view>  
</view>

  
